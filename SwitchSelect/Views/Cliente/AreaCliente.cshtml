@model SwitchSelect.Models.ViewModels.ClienteViewModel

<h2>Area do cliente</h2>

<div>
    <dl class="row">
        
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nome):
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        
        <hr />
    </dl>
    <a asp-controller="Cliente" asp-action="DadosPessoais" asp-route-id="@Model.Id" class="btn btn-primary">Dados pessoais</a>
    <a class="btn btn-primary">Endereço</a>
    <a class="btn btn-primary">Metodo de pagamento</a>
    <a class="btn btn-link">Voltar</a>
     
</div>
